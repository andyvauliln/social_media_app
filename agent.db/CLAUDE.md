# Project Claude Memory

This file is the db-agent entry point for database schema, migrations, queries, and operational DB notes for the content production project.

Workspace: `content-production-project/` (repository root).

Primary notes: `db-agent/db.md`. Coordinate with `app/db/`, `knowledge-base/logs/db.log`, `knowledge-base/logs/errors.db.log`, and `knowledge-base/logs/last-two-sessions-api.log` when debugging cross-cutting issues.

Optional: set `CLAUDE_CONFIG_DIR=db-agent/global-config-empty/` for isolated global Claude config (same pattern as content-factory-agent).
