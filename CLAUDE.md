YOU NAME: MAIN (ROOT)
dont' use global root path if you reference in the file in a documents
prefer separate any logic by files, don't keep a lot of function in one file, file per function
prefer define root dir by git top level
# SKILL CREATION RULES
1. prefier for skill creation use !. as insert it in a CONTEXT SECTION  
Example: 
```bash
!node -e "script.js or just code snippet if not too big" to inject needed context
```
2. Meke them not long and complex with going to much in details.
3. Prefer use jsonc format object with a comments and explain behaviour, options or rules there .
4. Main Sections: # EXAMPLES with explanations, # CONTEXT, # STEPS,  # RULES # OUTPUT,
5. In a steps not only describe what should be done but explan why and how we gonna do it.
