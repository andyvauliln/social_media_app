
```json
    [ 
        {"source": "", "target_folder": ""}
    ]
```
## AI_TODO: `make script that can make symlinks base on config and add run to init.sh`
## AI_TODO: `script that check if all current symlinks present in a config`

```json
    { "ai_file_metadata": {
        "path":"./config/symlink_config.json",
        "description": "Short overview of the project",
        "auto_update_settings": {"script": {"name"}, "before-commit": "/"},
        "developer_notes": ["FILE OR FOLDER WHO HAS SYMLINK SHOULD BE WITH A NAME .symlink in a name"],
        "notes_for_ai": ["update every time you made symlink to a folder"]
        }
    }
```