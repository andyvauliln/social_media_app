name: Skillshare Sync

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: runkids/setup-skillshare@v1
        with:
          source: ./skills
      - run: skillshare sync
