---
name: create-task.manager
description: Main command for creating tasks with a manager agent
argument-hint: "[prompt] [@user_name, @ai optional] [@when optional] [@priority optional] [@type optional] [@scope optional] [@plan optional]"
---

Run the create-task skill in `agents/agent.manager/.claude/skills/create-task/SKILL.md`.

